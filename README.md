# Графический калькулятор

Современный графический калькулятор на Flutter с поддержкой трех режимов работы, темной и светлой темы, и адаптивным дизайном для всех размеров экранов.

## Особенности

### 🎯 Три режима работы

1. **Обычный калькулятор**
   - Базовые математические операции (+, -, ×, ÷)
   - Работа со скобками
   - Поддержка десятичных чисел
   - История вычислений

2. **Инженерный калькулятор**
   - Все функции обычного калькулятора
   - Тригонометрические функции: `sin`, `cos`, `tan`
   - Логарифмические функции: `ln`, `log`
   - Квадратный корень: `sqrt`
   - Возведение в степень: `^`
   - Константы: `π` (пи), `e` (число Эйлера)
   - Проценты: `%`

3. **Графический калькулятор**
   - Построение графиков функций с переменной `x`
   - Интерактивное масштабирование (жестами или кнопками)
   - Перемещение графика
   - Поддержка всех математических функций
   - Координатная сетка с подписями осей

### 🎨 Дизайн

- **Темная и светлая тема** с переключателем
- **Адаптивный дизайн** для всех размеров экранов:
  - Мобильные устройства (< 600px)
  - Планшеты (600-1200px)
  - Десктопы (> 1200px)
- **Плавные анимации**:
  - Анимации нажатия кнопок
  - Плавное появление графика
  - Масштабирование с анимацией

### 🔧 Функциональность

- **Умный парсер выражений** с валидацией
- **Обработка ошибок** без крашей приложения
- **История вычислений** (последние 10 операций)
- **Красивое форматирование** чисел
- **Интуитивный интерфейс**

Приложение поддерживает три режима работы с удобным переключением через вкладки.

## 🎮 Использование

### Обычный калькулятор

1. Выберите вкладку "Обычный"
2. Введите выражение с помощью кнопок
3. Нажмите `=` для вычисления
4. Используйте `C` для очистки
5. Используйте `⌫` для удаления последнего символа

### Инженерный калькулятор

1. Выберите вкладку "Инженерный"
2. Используйте функции:
   - `sin(x)`, `cos(x)`, `tan(x)` для тригонометрии
   - `ln(x)`, `log(x)` для логарифмов
   - `sqrt(x)` для квадратного корня
   - `^` для возведения в степень
   - `π` и `e` для констант

### Графический калькулятор

1. Выберите вкладку "График"
2. Введите функцию с переменной `x`, например:
   - `x^2` - парабола
   - `sin(x)` - синусоида
   - `x*2+1` - прямая линия
   - `x^3-2*x` - кубическая функция
3. Нажмите `=` для построения графика
4. Используйте жесты:
   - **Pinch** для масштабирования
   - **Drag** для перемещения графика
5. Используйте кнопки управления:
   - `+` - увеличить масштаб
   - `-` - уменьшить масштаб
   - `⊞` - сбросить вид

### Переключение темы

Нажмите на иконку солнца/луны в правом верхнем углу для переключения между темной и светлой темой.

### История вычислений

Нажмите на иконку истории в левом верхнем углу, чтобы просмотреть последние вычисления.

## 📂 Структура проекта

```
lib/
├── main.dart                 # Точка входа, настройка приложения
├── app_theme.dart            # Управление темами (светлая/темная)
├── responsive_utils.dart     # Утилиты для адаптивного дизайна
├── calculator_engine.dart    # Движок вычислений и парсинг выражений
├── calculator_keyboard.dart  # Виджет кнопки калькулятора
├── basic_calculator.dart     # Обычный калькулятор
├── scientific_calculator.dart # Инженерный калькулятор
├── graph_calculator.dart     # Графический калькулятор
└── graph_widget.dart        # Виджет для отображения графика
```

## 🔬 Примеры выражений

### Базовые операции
```
2 + 3 * 4
(10 - 5) / 2
2.5 * 3.14
```

### Инженерные функции
```
sin(3.14159/2)
cos(0)
ln(e)
log(100)
sqrt(16)
2^8
```

### Графики
```
x^2
sin(x)
x^3 - 2*x
x*2 + 1
cos(x) * 2
```

## 🛠 Технологии

- **Flutter** - кроссплатформенный фреймворк
- **Dart** - язык программирования
- **Custom Paint** - для отрисовки графиков
- **Material Design 3** - современный дизайн

## 🎯 Планы на будущее

- [ ] Сохранение графиков
- [ ] Экспорт вычислений
- [ ] Дополнительные математические функции
- [ ] Поддержка комплексных чисел
- [ ] Режим программирования (HEX, BIN, OCT)
